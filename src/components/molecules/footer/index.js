import styled from "styled-components";

const Wrapper = styled.div`
    height: 100px;
    margin-top: 100px;
`;

function Footer () {
    return (
        <Wrapper>
        </Wrapper>
    );
}

export default Footer;